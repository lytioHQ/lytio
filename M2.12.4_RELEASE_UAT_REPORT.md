# M2.12.4 Release UAT Report

- 版本：M2.12.4 Business Memory v0（Release Closure）
- 日期：2026-08-19
- 结论：**READY_FOR_CUSTOMER_UAT**（P0=0，P1=1，P2=2，无阻塞）

---

## 1. 版本范围与代码状态

| 项 | 值 |
| - | - |
| HEAD / origin/main | `176d2d2`（fix: migration runner tolerate semicolons in comments） |
| 本次 Release 提交 | `88c85fa`（business memory layer）、`f6bec55`（memory 展示/robustness/i18n）、`176d2d2`（migrate.py 修复） |
| 工作区状态 | 无已跟踪文件改动（仅仓库根 untracked 报告文档） |
| 代码审计 | Phase 0 全过：`_m2124_test.py` 48/48、`_m2123_test.py` 回归 23/23、`tsc --noEmit`、`next build`、`git diff --check` |

本阶段未修改任何已验证业务契约；`result_json` 历史结构不变；AI 未参与任何计算/评分/完成判断。

## 2. Release Closure 执行结果

| 阶段 | 结果 | 证据 |
| - | - | - |
| Phase 3 Backup Gate | PASS | `lytio_before_m2.12.4.dump` + `.sql` 双格式备份，MD5 验证，报告 `M2.12.4_RELEASE_BACKUP_REPORT.md` |
| Phase 4 Migration 001–004 | PASS | `cd /app/migrations && DB_BACKUP_CONFIRMED=true python migrate.py`；新表/索引/计数/MD5 验证全过 |
| Phase 4 数据完整性 | PASS | migration 前后 analysis_runs count/max(id)、projects.latest_result_json MD5、action/verification 表数据不变 |
| Phase 5 Deploy | PASS | Railway 后端 deployment `c3afd4c3` SUCCESS；Vercel `www.lytio.co` 200，executive 路由 chunk 含 M2.12.4 Business Memory 标记 |
| Phase 6 Smoke Test | PASS | 真实 E2E 全链路通过（见第 3 节） |
| Phase 7 Customer UAT | PASS | 见第 4–7 节 |

### Migration 后基线（smoke 测试前，生产 DB）
- `analysis_runs` count=16 / max(id)=16；`projects`=20；`action_items`=4；`analysis_jobs`=12
- `result_json` MD5=`78f3ed1b2a87264f198f923212a29c56`；`projects.latest_result_json` MD5=`ab744da27af9240209fee90d4a9ac467`
- `business_memory` 表 + `ix_business_memory_project` 索引已存在

## 3. Smoke Test（真实生产 E2E）

测试账号（自动注册）：
- `smoke.m2124.0e2b350a@example.com`（user_id=29，project_id=25）
- `smoke.m2124.2b334583@example.com`（user_id=30，project_id=26）

测试数据：`%TEMP%\lytio_smoke_v1.xlsx`（2026-01~03，12 行）、`lytio_smoke_v2.xlsx`（2026-04~06，12 行）。

全链路校验结果（跨两轮真实运行 + 服务端落库核验，全部 PASS）：

| 环节 | 结果 | 说明 |
| - | - | - |
| health / register / upload v1 / project / link | PASS | 基础链路 |
| Health Scan（异步 job） | PASS | job→completed，AnalysisRun 创建 |
| timeline / executive / metrics / health_score | PASS | 200；computed_metrics 含 total_sales；health_score 存在 |
| Action Item 生成 | PASS | recommendation→action 创建成功 |
| Direction Analysis（growth_opportunity） | PASS | 新 AnalysisRun，未覆盖历史 |
| Upload v2 → Verification | PASS | POST 202 → job completed → run(verification, v2, parent 正确) |
| Comparison | PASS | 200；parent 关联正确；comparison_result 完整（run 25：verdict=`partially_effective`，metric_changes=6，recommendation_results=5，next_actions=4） |
| Business Memory | PASS | /memory 200、ready=true、metric/health/verification history、action_summary 齐备 |
| Memory Trend | PASS | health_score / total_sales 趋势 ≥2 点 |
| 历史报告持久化 | PASS | 退出重进仅读取历史 AnalysisRun，health_scan 唯一，未重新分析 |

AI job 耗时（DeepSeek 实时调用）：health_scan 69–114s；deep_analysis 85–113s；verification 60–104s。等待体验由前端阶段 checklist 覆盖（M2.10 已验证，本次回归通过）。

## 4. 用户旅程结果（UAT）

| 步骤 | 结果 |
| - | - |
| 注册 → 上传 → Health Scan | PASS |
| Business Health / Executive Report / 建议 | PASS |
| 验证优化效果（Verification） | PASS |
| Comparison（Before/After，指标变化+建议效果+下一步） | PASS |
| Business Memory（趋势/闭环/未解决事项） | PASS |

产品形态确认：已完成 Excel → Canonical Schema → Metric Engine（硬计算）→ Health Score（算法评分）→ Action（执行闭环）→ Verification（效果验证）→ Business Memory（企业长期记忆）的完整数据管道。

## 5. 五问复盘

1. **新增什么**：M2.12.4 增加 Business Memory v0 —— 每项目一条只读聚合记录（schema coverage、metric timeline、health score timeline、action summary、verification summary、unresolved issues），`GET /api/projects/{id}/memory` 与 trend 端点，Executive 页 Business Memory 卡片；迁移 004 建立 `business_memory` 表（幂等，无 backfill）。另包含 M2.12.3 未发布部分（Action Item 系统 + 前端展示）与 migrate.py 注释分号修复。
2. **增强哪个护城河**：Layer 5 Business Memory（企业长期经营记忆）。同时补全 Layer 4 Action System 的发布闭环，验证了「建议→行动→验证→沉淀」链路。
3. **是否影响客户测试**：否。Phase 0 回归全过；历史 result_json/契约/旧报告未改动；smoke 全链路 PASS；新增能力全部增量只读。
4. **距离完整闭环还差什么**：① Metric Engine 覆盖面（漏斗/转化/客户留存/CLV）；② Health Score 行业 Benchmark 与权重校准；③ Action System 的 owner/deadline/提醒与多周期追踪；④ Verification 的硬计算兜底（见 P1）；⑤ Business Memory 的跨周期记忆对话消费。
5. **下一阶段最高价值**：M2.13 首选 Verification 硬计算兜底 + Action 多周期追踪（把「验证效果」从 AI 依赖升级为指标引擎兜底），其次 Schema Mapping 用户确认交互（提升首传体验与商业化）。

## 6. 问题清单

- **P0**：无。
- **P1**：Verification AI 偶发空输出 → comparison 落为 `unable_to_verify` 空骨架（smoke 项目 25 出现一次，rj_len=172；项目 26 正常 rj_len=3274）。链路诚实降级、无 500、run 正确落库，但商业价值低。建议 M2.13：verification 增加 AI 输出重试，或先用 Metric Engine 计算 before/after metric_changes 作为硬计算兜底。
- **P2**：① 测试脚本曾用错误轮询端点（`/verification/{job_id}` 不存在，正确为 `/analysis/{job_id}`）——仅工具侧问题，已修正；② 脚本客户端 60s 读超时无重试，网络抖动会导致本地轮询中断——已加重试，建议后续 smoke 工具统一接入重试。

## 7. 结论

- Release Closure 全部通过：Backup → Migration(001–004) → Schema/数据验证 → Deploy → Smoke → Customer UAT。
- **READY_FOR_CUSTOMER_UAT**，可进入真实客户体验测试；未完成项不阻塞发布。
- 依据「未完成 Release Closure 禁止进入下一版本」：M2.12.4 Closure 已闭合，M2.13 规划可在客户 UAT 并行/之后启动，但需先解决 P1（Verification 硬计算兜底）再扩大验证范围。
