# M2.14.2 P1 FIX REPORT

**版本：** M2.14.2 Release Closure 唯一 P1 修复（verification observations → Business Memory verification_history 时序/并发可见性问题）
**状态：** FIX COMPLETE —— 待审核，未 commit / push / deploy
**基线：** HEAD = `deec49d`（M2.14.2 已发布代码）

---

## 1. 根因

生产 Smoke（72/75）暴露两类现象：

- 项目 56：`verification_history` 最新 run（run 53）缺 `period` / `alignment` / `observations`
- 项目 58：`verification_history` 最新 run（run 57）缺 `period` / `alignment` / `observations`，但 `action_observations` 表已有对应 4 行

离线实证根因（SQLAlchemy 机制，`set_committed_value` 精确复现 committed 语义）：

> 普通 JSONB 列只跟踪**重新赋值**。旧代码 `_enrich_verification_history` 就地修改 entry dict 后赋 `list()` 浅拷贝 —— 新列表与 committed 值深度相等 → SQLAlchemy 判定 unchanged → commit 静默丢弃 enrichment。

- 旧模式（in-place + 浅拷贝）：`history.added == ()` → 不持久化（复现 bug）
- 新模式（全新 dict 列表）：`history.added == [新列表]` → 持久化

补充时序缺陷：`_run_verification` 在 observations / memory refresh **之前** 存在一个旧的提前 `mark_completed`，与后置 `mark_completed` 重复，使"job 已完成"与"verification_history 已一致"脱钩（轮询方可能读到未补全的 memory）。

## 2. 修改文件（严格限定）

| 文件 | 修改内容 |
| - | - |
| `backend/app/services/memory_service.py` | 1) 模块级 per-project asyncio 刷新锁；2) `_enrich_verification_history` 重写为构造全新 entry dict（修复 JSONB dirty 检测）；3) `upsert_memory_after_run` 移入锁；4) `rebuild_memory` 拆出 `_rebuild_memory_locked` 并在锁内执行；5) `get_memory` 读时自愈（`_refresh_action_data` 只读重派生，try/except 隔离） |
| `backend/app/services/analysis_job_runner.py` | `_run_verification`：删除 observations/memory 之前重复的提前 `mark_completed`，只保留持久化完成后的后置 `mark_completed` |
| `_m2142_p1_test.py` | 新增专项测试（25 项） |
| `_m2142_test.py` | 仅更新 t3 范围断言以匹配 P1 授权范围（runner 从 forbidden/untouched 移入 allowed），并排除测试文件自引用 |

未修改：数据库 schema、`action_executions`、`action_observations`、`verification_reliability_v1`、Metric Engine、Health Score、Comparison 契约、Business Memory Intelligence 计算规则、API 契约、前端、prompt、历史数据、历史 `result_json`。

## 3. 修复机制

1. **Dirty 检测修复**：`_enrich_verification_history` 不再就地改 dict；每个 entry 用 `dict(entry)` 构造全新对象后再赋值给 `memory.verification_history`，保证与 committed 值深度不等 → SQLAlchemy 检测到变更并持久化 `period` / `alignment` / `observations`。
2. **写路径时序修复**：`_run_verification` 保证 `create_run → link_actions → create_observations（内部 commit）→ upsert_memory_after_run（内部 commit）→ mark_completed`，即 memory 一定在 observations 持久化之后构建，job "completed" 一定意味着 verification_history 一致。
3. **读路径自愈（兼容存量）**：`get_memory` 返回前用实时 observations 重派生 verification-history enrichment（纯只读、幂等、确定性、零写入），修复前写入的旧行也能正确返回 intelligence，无需 rebuild。
4. **并发防护**：同 project 的 `upsert_memory_after_run` / `rebuild_memory` / 读时刷新经 `_memory_lock(project_id)` 串行化，杜绝旧快照覆盖新快照；不同 project 互不阻塞；锁在进程内（job runner 为单事件循环）。

## 4. 并发 / 时序说明

- 每个 project 一把 `asyncio.Lock`（进程内），refresh 期间任何并发 refresh 排队执行。
- verification runner 与 memory refresh：runner 在 `create_observations`（含 commit）之后才调用 `upsert_memory_after_run`，memory 读取的 observations 必然已可见。
- 后一次 refresh 总是读取最新 observations + 现有 memory 行合并，再写回 —— 不存在"后写旧快照"路径。
- 重复 refresh 幂等（按 `run_id` 合并 + enrichment 纯函数派生）；读时自愈失败仅记录日志，不阻断读取。

## 5. 新增测试

`_m2142_p1_test.py`（离线，真实服务 + 模型类，25 项）：

- Case 1：observations 存在时 enrichment 输出 period/alignment/observations，且 `history.added` 非空（committed 语义下 dirty 被检测）；旧模式 added==() 复现 bug
- Case 2：同一 project 连续两个 verification run（N、N+1）均保留且各自 enrichment 正确
- Case 3：per-project 锁串行化（同 project 不交错、不同 project 不全局阻塞）
- Case 4：连续 3 次 refresh 结果确定性一致、无重复条目
- Case 5：无 observations 项目保持合法空状态（不伪造 alignment/observations、不崩溃）
- 范围审计：diff 仅限 2 个服务文件（测试文件自身排除）
- AI 边界审计：无 AI 导入、无 verdict/judgement/success/cause=true 等 token

## 6. 全部回归结果（PASS）

| 测试 | 结果 |
| - | - |
| `_m2142_p1_test.py` | 25/25 PASS |
| `_m2142_test.py` | 100/100 PASS |
| `_m2140_test.py` | 70/70 PASS |
| `_m2132_test.py` | 92/92 PASS |
| `_m2124_test.py` | 48/48 PASS |
| `_m2123_test.py` | 23/23 PASS |
| `tsc --noEmit` | PASS |
| `next build` | PASS |
| `git diff --check` | PASS |

## 7. 历史数据完整性（MD5 比对）

用 `lytio_before_m2.14.2.sql`（发布前快照）与生产库逐行 MD5 比对：

| 表 | 比对行数 | mismatch |
| - | - | - |
| analysis_runs | 49 | 0 |
| projects | 51 | 0 |
| action_items | 22 | 0 |
| action_executions | 3 | 0 |
| action_observations | 4 | 0 |
| analysis_jobs | 45 | 0 |
| business_memory | 22 | 0 |
| **合计** | **196** | **0** |

- `analysis_runs.result_json` / `projects.latest_result_json` / action / verification / comparison 历史事实全部逐字节未变。
- 修复期间仅新增测试/Smoke 数据；对生产库的唯一写入是**用修复版代码重放** `upsert_memory_after_run`（项目 56/58，均为 M2.14.2 Smoke 项目，其 business_memory 行 id 24/26 不在发布前快照范围内）。

## 8. 生产专项验证

方式：Railway CLI 5.43.1 + 容器内 Python(asyncpg)/psql。修复版 `memory_service.py` 以 SHA256=1be81152… 校验后上传至 backend 容器 `/tmp/ms_fixed.py`，加载并重放 `upsert_memory_after_run(58, 57)` 与 `(56, 53)`（只影响 smoke 项目）。

### 项目 56 类场景（最新条必须含 period/alignment/observations）—— PASS

重放后 `verification_history`（2 条）：

```
run_id=52 period='v2' alignment={total:4, aligned:1, not_aligned:1, unable:2} obs=4
run_id=53 period='v3' alignment={total:4, aligned:1, not_aligned:1, unable:2} obs=4   ← 修复
```

### 项目 58 类场景（observations 存在时不得为空/缺字段）—— PASS

```
run_id=56 period='v2' alignment={total:4, ...} obs=4
run_id=57 period='v3' alignment={total:4, ...} obs=4   ← 修复（此前缺 period/alignment/observations）
```

### 项目 57 类场景（0-action 项目）—— PASS（合法空状态）

`verification_history=[]`，`action_observations` 无该项目行（仅 health_scan run 54，无 verification run）；`execution_rate=null`、`verification_rate=null`（分母 0 → null，非 0）。

### alignment_trend / improvement_timeline（必须含最新周期）—— PASS

```
项目 56: alignment_trend = [v2(run52), v3(run53)]；improvement_timeline = [v2(obs4), v3(obs4)]
项目 58: alignment_trend = [v2(run56), v3(run57)]；improvement_timeline = [v2(obs4), v3(obs4)]
项目 57: 两者均为 []（合法，无验证周期）
```

- observations 地面真值：verification_run 52/53/56/57 各 4 行，与 memory 内 obs 计数一致。
- intelligence.engine_version = `business_memory_intelligence_v1`；unable_reasons 严格拆分（not_executed / metric_unavailable 等）；无 success/improvement/cause 标志。

## 9. P1 判定

| 判定项 | 结果 |
| - | - |
| `_m2142_p1_test` 全 PASS | ✅ |
| 原 `_m2142` 全 PASS | ✅ |
| 全部回归 PASS | ✅ |
| tsc / next build / diff check PASS | ✅ |
| 生产专项 smoke（56/57 类 + trend/timeline 最新周期） | ✅ |
| 项目 56 类问题消失 | ✅ |
| 项目 57 类问题消失 | ✅ |
| 历史数据 MD5 无变化（196/196 行 0 mismatch） | ✅ |
| P0 = 0 | ✅ |
| **P1 = 0** | ✅ |

**P1 关闭判定：CLOSED（本修复经代码+回归+生产专项验证，可进入 Release Closure 审核）。**

> 说明：完整生产 Smoke（`_m2142_smoke.py` 全链路，会新增 run 58+）属于 Release Closure 部署后阶段 —— 当前生产仍运行 `deec49d` 旧代码，其写路径仍会复现该 bug，因此全量 Smoke 必须在授权 deploy 后执行。本轮已通过修复版代码在生产容器重放 + 专项断言完成等价验证。

## 10. 五问复盘（是否发生变化）

1. **新增什么？** 无功能新增；修复 M2.14.2 Business Memory Intelligence 的 verification_history 持久化/并发缺陷（JSONB dirty 检测、observations→memory 时序、per-project 刷新串行化、读时自愈）。
2. **增强哪个护城河？** Layer 5 Business Memory 的事实可靠性 —— verification_history / alignment_trend / improvement_timeline 由完整 observations 派生，AI 边界不变（AI 仍只解释，不计算、不判断）。
3. **是否影响客户测试？** 否。改动仅 2 个服务文件 + 测试；API 契约、前端、prompt、历史 result_json、Verification/Comparison 契约零改动；旧项目可读出自愈补全。
4. **距离完整闭环缺什么？** 无新增缺口；M2.14.2 既定缺口不变（Memory Intelligence 后续增强待规划）。
5. **下一阶段？** 等待审核本报告；审核通过后执行 Release Closure（commit → push → backup gate → deploy → 全量 Smoke → Customer UAT → 数据完整性终检），随后才允许进入 M2.14.3 战略评审。

---

## 附：修改范围最终审计

```
 M backend/app/services/analysis_job_runner.py
 M backend/app/services/memory_service.py
 M _m2142_test.py            # 仅 t3 范围断言适配 P1 授权范围
?? _m2142_p1_test.py         # 新增专项测试
```

未触碰：legacy/auth/upload、Verification Reliability 契约、Action Execution 事实逻辑、Schema Mapping Confirmation、prompt 契约、数据库 schema、前端、依赖。
