# M2.14.2 Customer UAT Remediation — 整改报告

> 阶段：M2.14.2 Customer UAT 后续整改（收尾）
> 生产基线：HEAD `1dfcc09`（M2.14.2 Customer Experience Reconstruction Phase 1 已发布）
> 状态：**M2.14.2 Customer UAT Remediation — Ready for Review**
> 约束执行：本阶段未 commit / push / migration / deploy；未进入 M2.14.3。

---

## 一、UAT 原始反馈

第一次真实客户视角 UAT 暴露的核心反馈：

1. 「这个产品到底是 AI，还是其他东西？」——反复出现「代码计算、AI 只解释」的工程师语言，客户无法建立产品心智。
2. 「验证、字段识别这两块内容，包括里面的英文不明白什么意思。」——`pipeline`、`missing field`、`aligned`、`unable_to_verify`、`observation`、`target metric` 等直接暴露。
3. 「来源建议：rec_recovery · 来自分析 #303」——内部建议 id 与 run id 作为主视觉信息。
4. Demo 周期 1/2/3 健康度数字不同，但描述、执行摘要、关键发现、风险、建议、行动任务完全一样——严重破坏真实性。
5. 「总销售额等数据溢出数据框。」——大数字在窄屏溢出。
6. 「首页很单调，Demo 更单调，整个页面都是同样结构，没有变化。」——大量浅蓝/浅灰/白连续区块，视觉像长表格。
7. 「什么叫周期？谁产生的周期？为什么有三个周期？」——Demo 首次出现周期概念无解释。
8. 健康评分卡直接暴露 `60 + clamp(sales_growth, ±0.5) × 40`、`health_score_v1` 等工程信息。

---

## 二、P0 / P1 / P2 分级

| 级别 | 问题 | 处理 |
| --- | --- | --- |
| P0 | 产品定位认知混乱（AI vs 代码计算） | 全站统一「AI 驱动的经营分析与决策闭环」口径，消除对立的工程师表述（整改项 3/4/12） |
| P0 | Demo 三周期只是换数字 | 三周期改为真实业务事实驱动，narrative 随周期事实变化（整改项 6/7） |
| P1 | 客户语言工程化（pipeline / missing field / need 2 months / formula / engine version） | 全部翻译为业务语言，公式与引擎版本折叠（整改项 4/8） |
| P1 | Schema 字段识别工程化 | 统一「数据字段确认」语义，缺失字段改为「缺少必要字段：销售阶段」等（整改项 8） |
| P1 | Verification 英文不可理解 | aligned / unable / verdict / reliability 全部本地化（整改项 8） |
| P1 | Action 来源展示工程语言 | 「来源：销售分析建议」+「查看原始分析」，内部 id 入 tooltip（整改项 8） |
| P1 | Demo 周期概念未解释 | 周期切换器新增「经营周期」解释（整改项 6） |
| P1 | 数据溢出 | MetricCard 响应式缩放 / 换行，412–1440px 四语言溢出矩阵全 PASS（整改项 9） |
| P1 | 首页 / Demo 视觉单调 | 分区视觉角色与节奏调整、健康等级本地化、Memory 卡片信息替换（整改项 10） |
| P2 | 细节文案（health level 代码值、engineVersion 展示位、reliability 原文） | 枚举本地化与详情折叠（整改项 4/8） |

---

## 三、产品定位整改

- **口径统一**：全站不再把「代码计算 / AI 只解释」作为面向客户的主叙事；健康评分卡副标题改为「由代码从数据中计算，AI 仅解释原因与改善建议。」（`healthscore.subtitle`，四语言）。
- **行动语义**：Action 区块文案「把 AI 建议转成可追踪的行动；核对只记录事实变化，不判定任务是否完成」（`action.subtitle`）；执行记录 hint「对齐结果由系统按指标变化计算，AI 不判定任务是否完成，也不做因果断言」（`action.execution.hint`）。
- **验证语义**：验证页标题「验证优化效果」，副标题「这不是重新分析。上传执行建议后的最新数据，Lytio 会对比优化前后的变化，判断建议是否达到预期」（`verify.subtitle`）。
- **记忆语义**：「经营改善分析 —— 基于执行记录与验证事实的代码计算结果，AI 不参与判定」（`memory.intel.desc`）。
- 未引入任何未经数据证明的因果表述；禁止性措辞（因果断言、success 判定）保持代码级约束。

## 四、客户语言整改

- 新增约 20 个 `healthscore.*` key（`missingField` / `missingFieldHint` / `needTwoMonths` / `needTwoMonthsHint` / `dimField.*` / `dimExplain.*` / `formulaDetail` / `dimNote.computed` / `conf.*` / `dim.*`），四语言。
- `missing field: pipeline_stage` → **「缺少必要字段：销售阶段」+「当前文件中未找到该字段，暂时无法计算此维度；不影响其他可计算指标。」**
- `need at least 2 months (order_date + sales_amount)` → **「需要至少 2 个月的销售数据」+「当前分析需要订单日期和销售金额，用于比较销售增长变化。」**（以代码事实为准：sales_growth 需要 ≥2 个 distinct month）
- 公式（`100 - min(100, CV(monthly_sales) × 120)` 等）移入「查看计算方式」`<details>` 折叠区，主界面只显示维度名 + 字段说明。
- 引擎版本 `health_score_v1` 仅在折叠详情区显示（「引擎版本」标签仍在 details 内），主视图不再出现。
- 健康等级代码值（`Fair`/`Good`）在 BusinessHealthCard / BusinessMemoryCard / HealthScoreBreakdown 全部本地化（优秀/良好/一般/需关注/严重）。
- `verification_reliability_v1` → 「系统计算结果」；无法验证原因 `not_executed`→「尚未执行」、`metric_unavailable`→「找不到对应指标」、`insufficient_data`→「数据不足」。
- de 维度名 `Conversion-Stabilität` → `Konversionsstabilität`；Memory 标题四语言（企业经营记忆 / Business Memory / 経営メモリ / Geschäftsgedächtnis）。

## 五、首页整改

- 真实报告预览区改为基于**最新周期**生成：`buildDemoInsights(latest, T)` / `buildDemoRecs(latest, T)` / `buildDemoHealthCard(latest, T)`（此前使用顶层 narrative 快照）。
- BusinessHealthCard 等级本地化；SECURITY 区块背景由 `bg-muted` 调整为 `bg-canvas`，减少连续同色区块。
- 首页不出现旧「销售分析工作台」mock、不出现「30 秒体验完整分析」承诺（沿用 Phase 1 修复后的真实 fixture 预览）。

## 六、Demo 整改

- **周期导航**：新增「经营周期」解释「每上传一份新的销售数据并完成一次分析，就形成一个经营周期。切换周期可以看到业务变化和行动执行后的结果。」（`demo.period.explain`，四语言）。
- **信息架构重排**（对应客户自然认知）：
  1. 现在经营得怎么样？（健康度 + 核心指标）
  2. 为什么？（关键发现 + 风险）
  3. 接下来做什么？（建议 + Action）
  4. 做了之后怎么样？（Execution + Verification 前后对照表）
  5. 长期有没有改善？（Memory Intelligence）
- **验证区块**：新增 Before/After 指标变化表（指标 / 优化前 / 优化后 / 变化 / 方向），verdict、confidence、reliability 全部本地化；无验证的周期显示「该周期还没有验证数据」空状态（`demo.verification.none`）。
- **Action 来源链**：Demo 的 Action 显示「来源：销售分析建议 + 查看原始分析」，tooltip 保留 `rec id · run id`（与生产组件同逻辑）。
- 技术说明（source_commit / engine_version / 公式）不进入主视觉。

## 七、三周期真实性整改

- Demo fixture（`demo_result.json`）三周期由**真实不同的业务事实**驱动，全部来自生产流水线口径（canonical schema → metric engine → health score）：

| 周期 | total_sales | 订单量 | 客单价 | 客户数 | 客户集中度 | 销售增长 | 健康度 | 覆盖率 | 置信度 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 周期1 | 380,000.01 | 40（估算） | 9,500.00 | 8 | 22.1% | 无法计算（仅 1 个月） | 66.9 一般 | 60% | 高 |
| 周期2 | 600,000.02 | 80（估算） | 7,500.00 | 8 | 19.4% | −42.1% | 64.5 一般 | 80% | 中 |
| 周期3 | 910,000.02 | 120（估算） | 7,583.33 | 8 | 16.4% | +40.9% | 75.0 良好 | 80% | 高 |

- **叙事随周期变化**：每个周期独立 `summary_key`（`demo.summary.p1/p2/p3`）与独立 params（销售额 / 增长率 / 健康度 / 集中度…）；`demo/index.ts` 新增 `DemoPeriodNarrative`，`buildDemoInsights / buildDemoRisks / buildDemoRecs / buildDemoActions / buildDemoExecutiveSummary / buildDemoHealthCard` 全部改为按周期读取（`periodNarrative()`），不再全局共用同一份文本。
- 周期3 才出现验证：run 401（parent 303），verdict=部分达到预期，metric_changes 代码计算：total_sales +40.9%（improved）、AOV +1.1%（unchanged）。
- Memory：health_history 66.9 → 64.5 → 75；action_summary total=4 / completed=2 / verified=2；verification_history alignment=aligned / observations=2；open_loops=2（未执行行动 + 长期未解决问题）。
- **真实性断言**（`period_authenticity.mjs`，zh 文案级比对）：period1 ≠ period2 ≠ period3（summary / insights / risks / recs / actions / verification 均不同）全 PASS。健康度描述不再是「换数字不换故事」。
- 周期1 增长维度 unavailable 是 fixture 内真实计算状态（仅 1 个月数据），不是人为伪造。

## 八、Verification / Schema / Action 整改

- **Verification**：`aligned`→「方向一致」、`not_aligned`→「方向相反」、`unable_to_verify`→「无法验证」；verdict 四态「达到预期 / 部分达到预期 / 未达到预期 / 暂无法验证」；reliability→「系统计算结果」；Demo 验证表方向列本地化（上升 / 下降）。
- **Schema**：字段确认语义统一为「数据字段确认」（沿用 M2.13.1 的字段级翻译，缺失字段展示「缺少必要字段：销售阶段」等）；未引入新 UI 结构，未改动后端 schema 契约。
- **Action**：`来源建议: rec_recovery · 来自分析 #303` → **「来源：销售分析建议 · 查看原始分析」**（生产 `Link` 跳转原报告，Demo 展示文本；内部 `recommendation_id · #source_run_id` 保留在 `title` tooltip 审计层）。新增 `action.sourceRecommendation` / `action.viewOriginalAnalysis` key。
- 三概念边界保持并在文案中明示：建议 ≠ 已执行（Action 状态机）；执行记录 ≠ 已验证成功（`action.verification.desc`）；验证结果 ≠ 因果证明（`action.execution.hint`）。

## 九、数据溢出整改

- `MetricCard.tsx`：数值从固定 `text-[30px]/[32px]` 改为 `text-[clamp(1.125rem,4.2vw,2rem)]`，容器 `min-w-0` + `max-w-full`，trend 符号 `shrink-0`，长数字自动缩放/换行，不覆盖其他内容。
- 溢出矩阵（zh/en/ja/de × 412/768/1280px，另复验 1440px）：所有核心 card 无横向溢出（docScrollW=397 < 412，clippedCards=0），证据见 `ov2_demo_{lang}_412.html`。
- 中文大数字（如 910,000.02）在窄屏按比例缩放，不截断关键数字。

## 十、视觉整改

- 健康评分卡主视图简化：只显示总分、等级（本地化）、覆盖率、置信度；维度折叠显示「查看计算方式」（含公式、字段、引擎版本）。
- 首页 SECURITY 区块背景调整，减少浅色连续大区块；真实报告预览跟随最新周期数据。
- Demo 周期切换器升级为卡片容器 + 业务解释，区块间距由 `space-y-10` → `space-y-12`，5 屏信息层级明确。
- 视觉节奏：本阶段以「信息层级 + 折叠 + 数据表格 + 状态标签」为主，未引入大图；整体视觉升级（分区背景/渐变/动效）列入首次客户测试后（见第十五节）。

## 十一、Demo ↔ Production Parity

- Demo 继续使用生产组件：`BusinessHealthCard`、`BusinessHealthCard` 内 `HealthScoreBreakdown`、`MetricGrid`/`MetricCard`、`ExecutiveSummaryCard`、`InsightList`、`RiskList`、`RecommendationList`、`BusinessActions`、`BusinessMemoryCard`、`VerificationSection`（Demo 页新增，使用与生产一致的 verdict/confidence/reliability i18n key）。
- Demo fixture 与生产口径一致：schema_version=`canonical_sales_v1`、engine_version=`metric_engine_v1`、health_score_engine=`health_score_v1`、reliability=`verification_reliability_v1`、intelligence=`business_memory_intelligence_v1`。
- `demo:check` PASS：fixture 04af2b0 · 3 periods · 18 i18n keys referenced。
- 不存在生产没有的指标（无旧 mock 如 Active SKUs）；不存在 `confidence=low` 的人为展示。
- source_commit=`04af2b0`（当前发布链）；data_md5=`9937732909c23ab8d2e0f169a8586b1f`；样例 `backend/demo/sample_sales_data.xlsx`。
- 四语言数值完全一致（i18n 只承载文案，数值存于 fixture）。

## 十二、AI 边界审计

- AI 未参与：字段识别、confidence、normalization、冲突检测、指标计算、健康评分、alignment、verdict、intelligence rates、执行/验证判定。
- 代码侧复核：本阶段全部修改位于前端 i18n / 展示组件 / Demo fixture；后端、数据库、migration 零修改。
- 文案侧复核：所有新增客户语言均保持「系统计算事实 + AI 解释」口径；未出现「导致增长」「证明有效」「Action 带来提升」等因果表述。
- 未生成任何新指标数字：三周期数字全部来自 fixture 的真实计算快照。

## 十三、测试结果

| 测试 | 结果 |
| --- | --- |
| `npm run demo:check` | PASS — fixture 04af2b0 · 3 periods · 18 i18n keys |
| `npx tsc --noEmit` | PASS |
| `npx next build` | PASS（全部路由构建成功） |
| `git diff --check` | PASS |
| 四语言文案断言（`lang_verify.js`） | 全部 PASS（zh「missing field」仅在「查看计算方式」折叠区内 1 次，属预期；「Business Memory」主视图消失；Good 可见出现 0 次；de `Konversionsstabilität`/`Geschäftsgedächtnis`；ja `経営メモリ`；en `Missing required field`） |
| 周期真实性断言（`period_authenticity.mjs`） | PASS — period1 ≠ period2 ≠ period3（summary/insights/risks/recs/actions/verification 全维度不同） |
| 溢出矩阵（四语言 × 412/768/1280/1440px） | PASS — 无横向溢出、无 card 裁剪 |
| 首页/Demo 本地渲染（zh/en/ja/de） | PASS — 截图见第十四节 |

## 十四、截图证据

- 四语言 Demo 截图：`C:\Users\徐捷\AppData\Local\Temp\lytio_visual\local_demo_{zh,en,ja,de}.png`（+ `.html`）
- 四语言 Landing 截图：`local_landing_{zh,en}.png`、`local_landing_zh2.png`
- 412px 溢出复验：`ov2_demo_{zh,en,ja,de}_412.html`（docScrollW=397 < 412，clippedCards=0）
- zh 主视图已目视确认：健康评分卡 75.0 / 良好 / 维度本地化 / 公式折叠；「Business Memory」不再作为主标签。

## 十五、修改文件清单（本阶段，工作区未 commit）

| 文件 | 修改内容 |
| --- | --- |
| `frontend/src/lib/i18n.ts` | 新增 ~20 个 `healthscore.*` key、`demo.period.explain`、`demo.summary.p1/p2/p3`、`demo.verification.*`、`action.sourceRecommendation`/`action.viewOriginalAnalysis`、`memory.intel.unable_reason.*`、`verifyReport.reliability.*` 等，四语言；de 维度名修正 |
| `frontend/src/lib/demo/demo_result.json` | 三周期独立 narrative / summary_key / params；周期3 验证与 Memory 事实 |
| `frontend/src/lib/demo/index.ts` | `DemoPeriodNarrative`、`periodNarrative()`、全部 builder 按周期读取；summary `{level}` 本地化 |
| `frontend/src/app/demo/page.tsx` | 周期解释、5 屏信息架构、VerificationSection（Before/After 表）、按周期渲染 |
| `frontend/src/components/landing/LandingPage.tsx` | 真实报告预览跟随最新周期；SECURITY 区块背景 |
| `frontend/src/components/business/BusinessHealthCard.tsx` | 健康等级本地化 |
| `frontend/src/components/business/BusinessMemoryCard.tsx` | 等级/reliability/unable_reason/周期引用数本地化，移除「引擎 v0」主展示 |
| `frontend/src/components/business/BusinessActions.tsx` | 来源改为「销售分析建议 + 查看原始分析」，内部 id 入 tooltip |
| `frontend/src/components/ui/MetricCard.tsx` | 响应式数值缩放，防溢出 |
| `frontend/src/components/business/HealthScoreBreakdown.tsx` | 全量重写：等级本地化、公式/引擎版本折叠、缺失字段与 2 个月提示客户语言化（HEAD 中为 Phase 1 版本，本轮重写） |
| 文档 | `CUSTOMER_LANGUAGE_MAP.md`（新增）、`M2.14.2_CUSTOMER_UAT_REMEDIATION_REPORT.md`（本文） |

后端 / 数据库 / migration / 历史 result_json / verification-comparison 契约：**零修改**。

## 十六、未解决问题（留到首次客户测试后，本阶段不处理）

- 首页整体视觉升级（分区背景材质、渐变、光晕、图表可视化、动效）。
- Demo 与生产组件的更深层复用（如统一 preview 组件层）。
- 真实免注册 Demo（当前 Demo 为只读样例，不做真实上传分析）。
- CTA 结构优化（「尝试演示」与「开始你的分析」的关系与层级）。
- 字段确认 UI 的进一步业务化（同义词学习、行业模板，属后续版本）。

## 十七、下一步建议

1. 由徐捷作为第一个模拟客户，按 `FIRST_CUSTOMER_UAT_GUIDE.md` Part A（盲测）从首页开始完整走一遍，再按 Part B 核对。
2. 重点观察：首次进入是否知道下一步做什么；是否理解「经营周期」；健康评分是否可信；Demo 三周期故事是否连贯；Memory 是否让人想继续使用。
3. 完成后再决定是否进入 M2.14.3 战略评审。

---

## 收尾状态

- 工作区停在：**M2.14.2 Customer UAT Remediation — Ready for Review**
- 本阶段：无 commit / push / migration / deploy；未进入 M2.14.3。
- 历史事实保护：analysis_runs / projects.latest_result_json / action_items / action_executions / action_observations / verification / comparison / business_memory 均未在本阶段触碰。
