# M2.12.4 Release — Production Backup Report

- 时间：2026-08-19 13:24–13:25 (+08:00)
- 环境：Railway `truthful-success` / `production` / `Postgres`（PostgreSQL 18.4）
- 备份方式：Railway SSH → Postgres 容器内 `pg_dump`（custom + plain SQL 双格式）
- 状态：**BACKUP_STATUS=READY**，可恢复性已验证

## 备份文件

| 文件 | 大小 | 远端 MD5 | 本地 MD5 | 一致 |
| - | - | - | - | - |
| `lytio_before_m2.12.4.dump`（custom，gzip） | 92,527 B | `ec5666a0c28984ab4072ebf8ca350ed7` | `EC5666A0C28984AB4072EBF8CA350ED7` | ✅ |
| `lytio_before_m2.12.4.sql`（plain） | 344,044 B | `d0852c00b2e3fc975683420e8580f2f5` | `D0852C00B2E3FC975683420E8580F2F5` | ✅ |

本地路径：`D:\Users\徐捷\Documents\lytio\lytio_before_m2.12.4.dump` / `.sql`
远端保留：Postgres 容器 `/tmp/lytio_before_m2.12.4.dump` / `.sql`（作为 in-place restore point）

## 可恢复性验证

- `pg_restore -l`（custom dump）：TOC 84 条目，Format CUSTOM，Dump Version 1.16-0 ✅
- plain SQL dump：标准 PostgreSQL dump 头（含 `\restrict`/`\unrestrict` 会话授权）✅

## 迁移前基线（Phase 4 对比基准）

- `analysis_runs`：count=16，max(id)=16
- `projects`：count=20；`action_items`：count=4；`analysis_jobs`：count=12
- JSON 合法性：`analysis_runs.result_json` 0 条非法；`projects.latest_result_json` 0 条非法
- MD5 基线：
  - `analysis_runs.result_json` 聚合 = `78f3ed1b2a87264f198f923212a29c56`
  - `projects.latest_result_json` 聚合 = `ab744da27af9240209fee90d4a9ac467`

## 说明

- 备份在 push（f6bec55）之后、migration 执行之前完成；期间后端自动部署曾通过
  `init_db()/create_all` 预建 `business_memory` 表（空表），正式 `004_business_memory.sql`
  随后经 `migrate.py` 幂等补齐正式索引 `ix_business_memory_project`，见
  `M2.12.4_IMPLEMENTATION_REPORT.md` / Release 报告。
- 发现并修复：`migrate.py` 按 `;` 分句会被 SQL 注释内分号截断（004 注释
  "table per project; no backfill"），已修复为剥离 `--` 注释后再分句
  （commit `176d2d2`），001–004 全量幂等重跑成功。
